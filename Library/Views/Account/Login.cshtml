@model Itera.Fagdag.November.ViewModels.LoginViewModel

@{
    ViewBag.Title = "Нэвтрэх хэсэг";
}

<hgroup class="title">
    <h1>@ViewBag.Title</h1>
</hgroup>
<div class="row-fluid">
    <div class="span6">

        <section id="loginForm">
            @using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl }))
            {
                
                <legend>Хувийн мэдээллээ ашиглан нэвтэрнэ үү</legend>
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)
                <fieldset class="form-horizontal">
                    <div class="control-group">
                        @Html.LabelFor(m => m.UserName, new { @class = "control-label" })
                        <div class="controls">
                            @Html.TextBoxFor(m => m.UserName)
                            @Html.ValidationMessageFor(m => m.UserName, null, new { @class = "help-inline" })
                        </div>
                    </div>
                    <div class="control-group">
                        @Html.LabelFor(m => m.Password, new { @class = "control-label" })
                        <div class="controls">
                            @Html.PasswordFor(m => m.Password)
                            @Html.ValidationMessageFor(m => m.Password, null, new { @class = "help-inline" })
                        </div>
                    </div>
                    <div class="control-group">
                        <div class="controls">
                            <label class="checkbox">
                                @Html.CheckBoxFor(m => m.RememberMe)
                                @Html.LabelFor(m => m.RememberMe)
                            </label>
                        </div>
                    </div>
                    <div class="form-actions no-color">
                        <input type="submit" value="Нэвтрэх" class="btn" />
                    </div>
                </fieldset>
                <p>
                    @Html.ActionLink("Бүртгүүлэх", "Register") бол энд дарна уу.
                </p>
            }
        </section>
    </div>
    <div class="span6">
        <section id="socialLoginForm">

            @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
        </section>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}